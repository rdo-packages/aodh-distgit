- project:
    check:
      jobs:
        - legacy-weirdo-dlrn-master-packstack-scenario001:
            dependencies:
              - legacy-DLRN-rpmbuild
            voting: false
            branches: ^(rpm-)?master$
